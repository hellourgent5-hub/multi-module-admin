rm -f render.yaml

cat > render.yaml <<'EOF'
services:
  - type: web
    name: multi-module-admin
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s build
    rootDir: .
    envVars:
      - key: NODE_VERSION
        value: 18
EOF
